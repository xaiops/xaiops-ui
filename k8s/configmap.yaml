apiVersion: v1
kind: ConfigMap
metadata:
  name: app-config
  labels:
    app: mortgage-assistant-ui
data:
  langgraph-api-url: "http://host.containers.internal:2024"
  api-base-url: "https://your-api.example.com"
  # Add other configuration values as needed
