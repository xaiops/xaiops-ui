apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: xaiops-ui-route
  labels:
    app: xaiops-ui
spec:
  to:
    kind: Service
    name: xaiops-ui-service
    weight: 100
  port:
    targetPort: http
  tls:
    termination: edge
    insecureEdgeTerminationPolicy: Redirect
  wildcardPolicy: None
