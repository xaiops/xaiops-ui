apiVersion: v1
kind: Service
metadata:
  name: mortgage-assistant-ui-service
  labels:
    app: mortgage-assistant-ui
spec:
  type: ClusterIP
  ports:
  - name: http
    port: 80
    targetPort: 8080
    protocol: TCP
  selector:
    app: mortgage-assistant-ui
